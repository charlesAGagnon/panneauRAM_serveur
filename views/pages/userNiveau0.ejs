<!DOCTYPE html>
<html lang="fr" data-theme="light">
<head>
    <%- include('../partials/head') %>
</head>
<body>
    <div class="min-h-screen py-12 px-4 sm:px-6 lg:px-8">
        <div class="max-w-4xl mx-auto">
            <!-- Header Card -->
            <div class="card bg-base-100 shadow-2xl mb-6 border-t-4">
                <div class="card-body">
                    <div class="flex items-center justify-between flex-wrap gap-4">
                        <div>
                            <h1 class="text-4xl font-bold bg-gradient-to-r from-primary to-primary bg-clip-text text-transparent">
                                Bonjour, <%= user %> !
                            </h1>
                            <p class="text-gray-600 mt-2">Tableau de bord - <%= typeAcces || 'Invité' %></p>
                        </div>
                        <div class="badge badge-lg gap-2">
                            <%= typeAcces || 'Invité' %>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Alert Info -->
            <div class="alert shadow-lg mb-6">
                <h3 class="font-bold">Accès en lecture seule</h3>
                <div class="text-xs">Vous pouvez consulter tous les Raspberry Pi mais sans possibilité de modifier les configurations.</div>
            </div>

            <!-- Raspberry Pi Access -->
            <div class="card bg-base-100 shadow-2xl mb-6">
                <div class="card-body">
                    <h2 class="card-title text-2xl mb-4">
                        Accès aux Raspberry Pi (Lecture seule)
                    </h2>
                    <div class="grid grid-cols-2 md:grid-cols-3 gap-3">
                        <a href="/raspberrypi1?user=<%= user %>&niveau=0&typeAcces=<%= typeAcces %>" class="btn btn-outline gap-2">Pi 1</a>
                        <a href="/raspberrypi2?user=<%= user %>&niveau=0&typeAcces=<%= typeAcces %>" class="btn btn-outline gap-2">Pi 2</a>
                        <a href="/raspberrypi3?user=<%= user %>&niveau=0&typeAcces=<%= typeAcces %>" class="btn btn-outline gap-2">Pi 3</a>
                        <a href="/raspberrypi4?user=<%= user %>&niveau=0&typeAcces=<%= typeAcces %>" class="btn btn-outline gap-2">Pi 4</a>
                        <a href="/raspberrypi5?user=<%= user %>&niveau=0&typeAcces=<%= typeAcces %>" class="btn btn-outline gap-2">Pi 5</a>
                        <a href="/raspberrypi6?user=<%= user %>&niveau=0&typeAcces=<%= typeAcces %>" class="btn btn-outline gap-2">Pi 6</a>
                    </div>
                    <div class="alert mt-4">Mode consultation uniquement - Aucune commande ne peut être envoyée</div>
                </div>
            </div>

            <!-- Action Buttons -->
            <div class="card bg-base-100 shadow-xl">
                <div class="card-body">
                    <div class="card-actions justify-end">
                        <a href="/" class="btn btn-outline gap-2">Déconnexion</a>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <%- include('../partials/footer') %>
</body>
</html>
